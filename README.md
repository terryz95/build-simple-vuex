# build-a-simple-vuex

手写一个简单的Vuex

[code](./src/store/simple-vuex.js)

✅ 提供`Vue.use()`注册

✅ `state`响应式；`getters`、`commit`、`dispatch`方法实现

❌ 未实现`module`分模块；`getters`不支持传入`rootState`

❌ 未实现`mapApi`

基于`vue-cli`，启动预览请使用`yarn serve`/`npm run serve`。

